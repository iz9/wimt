# EventStorming — Time Tracker App

Дата: 19.11.2025  
Цель: выявить доменные события, агрегаты, команды и бизнес-правила для приложения трекинга времени по категориям

---

## 1️⃣ User Journey (Сценарий использования)

1. Пользователь создаёт категорию: «Рутина», «Работа», «Хобби»
2. Пользователь видит категории на главном экране (в виде прямоугольников)
3. Пользователь нажимает на категорию → стартует таймер (создаётся сессия)
4. Пользователь может **поставить на паузу** или **остановить** сессию
5. После `Stop` сессия фиксируется с сегментами времени
6. Пользователь может просматривать **статистику**
7. Пользователь может **экспортировать данные в MD**

---

## 2️⃣ Domain Events (События домена)

| Событие           | Описание                                    |
| ----------------- | ------------------------------------------- |
| `CategoryCreated` | Пользователь создал категорию               |
| `SessionStarted`  | Пользователь начал сессию по категории      |
| `SessionPaused`   | Пользователь поставил сессию на паузу       |
| `SessionResumed`  | Пользователь возобновил сессию              |
| `SessionStopped`  | Пользователь закончил сессию                |
| `SegmentTooShort` | Сегмент был слишком короткий для сохранения |
| `SessionExported` | Пользователь экспортировал сессию в MD      |

---

## 3️⃣ Commands (Команды)

| Команда                    | Вызывает событие  |
| -------------------------- | ----------------- |
| `CreateCategory(name)`     | `CategoryCreated` |
| `StartSession(categoryId)` | `SessionStarted`  |
| `PauseSession(sessionId)`  | `SessionPaused`   |
| `ResumeSession(sessionId)` | `SessionResumed`  |
| `StopSession(sessionId)`   | `SessionStopped`  |
| `ExportSession(sessionId)` | `SessionExported` |

---

## 4️⃣ Aggregates / Entities

### Category (Aggregate Root)

- ID (ULID)
- Name
- Методы: создание категории, редактирование (опционально)

### Session (Aggregate Root)

- ID (ULID)
- CategoryId
- Сегменты (`SessionSegment[]`)
- Методы: `startSegment`, `pauseSegment`, `stopSession`

### SessionSegment (Entity)

- ID
- StartedAt: DateTime
- StoppedAt?: DateTime

---

## 5️⃣ Value Objects

- `DateTime` — для отметки времени
- `ULID` — уникальные идентификаторы
- `Duration` (опционально) — длительность сегмента

---

## 6️⃣ Business Rules / Policies

- Один активный сегмент на сессию
- Нельзя создавать перекрывающиеся сегменты
- Сегменты < 300ms не сохраняем (`SegmentTooShort`)
- `Stop` завершает сессию полностью
- `Resume` создаёт новый сегмент

---

## 7️⃣ Read Models / Queries

- `CategorySummary` — суммарное время по категориям
- `SessionTimeline` — список сегментов по времени
- `ExportedSession` — MD-формат для экспорта

---

## 8️⃣ EventStorming Flow (текстовое представление)

```md
User Action → Command → Aggregate → Domain Event → Policy / Business Rule → Read Model 1. Create Category
User clicks “New Category” → CreateCategory(name) → Category → CategoryCreated → Update CategorySummary 2. Start Session
User clicks category → StartSession(categoryId) → Session → SessionStarted → StartSegment → Update SessionTimeline 3. Pause Session
User clicks pause → PauseSession(sessionId) → Session → SessionPaused → SegmentStopped → Update SessionTimeline 4. Resume Session
User clicks resume → ResumeSession(sessionId) → Session → SessionResumed → New Segment Started → Update SessionTimeline 5. Stop Session
User clicks stop → StopSession(sessionId) → Session → SessionStopped → Aggregate stops session → Update SessionTimeline 6. Export
User clicks export → ExportSession(sessionId) → Session → SessionExported → Generate MD file
```

---

### 🔹 Примечания

- Все события **генерируются через Aggregate Root**
- Доменные модели **не знают про UI, инфраструктуру или DI**
- DateTime используется как Value Object для всех отметок времени
- Все бизнес-правила проверяются внутри агрегатов

---

> Этот документ является живым журналом EventStorming для приложения.  
> Каждое последующее изменение или уточнение логики сессий и категорий фиксируется здесь для поддержания контекста.
